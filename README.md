# React + TypeScript + Vite

First challenge is stopwatch

in this challenge the main issue was stoping the setInterval using clear interval. It was not persisting the current value.
so for this i use 
 # useRef Hook

 useRef start with a initial value and store the current value in hookVariable.current